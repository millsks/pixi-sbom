# Companion manifest for the hand-composed source-packages lockfile (pixi-build shapes
# taken from rattler's conda-lock v7 test data). Not itself lockable.
[workspace]
name = "source-packages"
version = "0.9.0"
channels = ["conda-forge"]
platforms = ["linux-64"]
preview = ["pixi-build"]
