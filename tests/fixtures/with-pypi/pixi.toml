[workspace]
name = "with-pypi"
version = "0.4.0"
channels = ["conda-forge"]
platforms = ["linux-64", "osx-arm64"]

[dependencies]
python = "3.12.*"

[pypi-dependencies]
six = "*"

[feature.web.pypi-dependencies]
requests = ">=2.32"

[environments]
web = ["web"]
