A miniature rattler package cache (`pkgs/<name>-<version>-<build>/info/`) matching the `conda-only`
fixture's linux-64 packages, for testing `--fetch-licenses` offline. `zlib` has a full `about.json` and a
license file; `libzlib` has an `about.json` without URLs and no `licenses/` directory.
